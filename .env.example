VITE_API_URL=http://localhost:5000/api
# aaa

# On Vercel (production), set this to the relative path "/api" instead of
# the Render URL directly - vercel.json proxies it so the login cookie is
# same-origin (avoids third-party cookie blocking in Incognito/strict browsers).
# VITE_API_URL=/api